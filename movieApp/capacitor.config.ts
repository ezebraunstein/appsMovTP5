import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.movieapp',
  appName: 'movieApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
